{
  "name": "colonist-public-qa",
  "version": "1.0.0",
  "private": true,
  "description": "A small Playwright smoke suite for Colonist's public web experience.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.15.0"
  }
}
